// Code generated by github.com/arran4/go-subcommand/cmd/gosubc. DO NOT EDIT.

package main

import (
	"testing"
	{{- $hasDuration := false }}
	{{- range .Parameters}}
	{{- if .IsDuration }}
	{{- $hasDuration = true }}
	{{- end }}
	{{- end }}
	{{- if $hasDuration }}
	"time"
	{{- end }}
)

func TestRoot_Execute(t *testing.T) {
	cmd, err := NewRoot("test", "", "", "")
	if err != nil {
		t.Fatalf("Failed to create root command: %v", err)
	}

	{{if .FunctionName}}
	called := false
	cmd.CommandAction = func(c *RootCmd) error {
		called = true
		return nil
	}

	args := []string{}
	{{- range .Parameters}}
	{{- $param := . }}
	{{- if and (not .HasGenerator) (not .IsPositional)}}
	args = append(args, "{{.PrimaryFlagName}}")
		{{- if and (ne .BaseType "bool") (not .IsBool)}}
			{{- if eq .BaseType "string"}}
	args = append(args, "test")
			{{- else if or (eq .BaseType "int") (eq .BaseType "int64")}}
	args = append(args, "1")
			{{- else if eq .BaseType "float64"}}
	args = append(args, "1.5")
			{{- else if .IsDuration}}
	args = append(args, "1s")
			{{- else if .IsWriter}}
	args = append(args, "-")
			{{- else if .IsReader}}
	args = append(args, "-")
			{{- end}}
		{{- end}}
	{{- end}}
	{{- end}}
	{{- range .Parameters}}
	{{- $param := . }}
	{{- if and (not .HasGenerator) .IsPositional}}
		{{- if .IsVarArg}}
			{{- if gt .VarArgMin 0}}
				{{- range until .VarArgMin}}
					{{- if eq $param.BaseType "string"}}
	args = append(args, "test")
					{{- else if or (eq $param.BaseType "int") (eq $param.BaseType "int64")}}
	args = append(args, "1")
					{{- else if eq $param.BaseType "float64"}}
	args = append(args, "1.5")
					{{- else if $param.IsBool}}
	args = append(args, "true")
					{{- else if $param.IsDuration}}
	args = append(args, "1s")
					{{- else if $param.IsWriter}}
	args = append(args, "-")
					{{- else if $param.IsReader}}
	args = append(args, "-")
					{{- end}}
				{{- end}}
			{{- end}}
		{{- else}}
			{{- if eq .BaseType "string"}}
	args = append(args, "test")
			{{- else if or (eq .BaseType "int") (eq .BaseType "int64")}}
	args = append(args, "1")
			{{- else if eq .BaseType "float64"}}
	args = append(args, "1.5")
			{{- else if .IsBool}}
	args = append(args, "true")
			{{- else if .IsDuration}}
	args = append(args, "1s")
			{{- else if .IsWriter}}
	args = append(args, "-")
			{{- else if .IsReader}}
	args = append(args, "-")
			{{- end}}
		{{- end}}
	{{- end}}
	{{- end}}

	err = cmd.Execute(args)
	if err != nil {
		t.Errorf("Unexpected error: %v", err)
	}
	if !called {
		t.Error("CommandAction was not called")
	}

	{{range .Parameters}}
	{{- $param := . }}
	{{- if and (not .HasGenerator) (not .HasCustomParser)}}
	{{- if .IsPositional}}
		{{- if .IsVarArg}}
			{{- if gt .VarArgMin 0}}
	if len(cmd.{{.Name}}) != {{.VarArgMin}} {
		t.Errorf("Expected {{.Name}} to have length {{.VarArgMin}}, got %d", len(cmd.{{.Name}}))
	}
				{{- range $i := until .VarArgMin}}
					{{- if eq $param.BaseType "string"}}
	if cmd.{{$param.Name}}[{{$i}}] != "test" {
		t.Errorf("Expected {{$param.Name}}[%d] to be 'test', got '%v'", {{$i}}, cmd.{{$param.Name}}[{{$i}}])
	}
					{{- else if or (eq $param.BaseType "int") (eq $param.BaseType "int64")}}
	if cmd.{{$param.Name}}[{{$i}}] != 1 {
		t.Errorf("Expected {{$param.Name}}[%d] to be 1, got '%v'", {{$i}}, cmd.{{$param.Name}}[{{$i}}])
	}
					{{- else if eq $param.BaseType "float64"}}
	if cmd.{{$param.Name}}[{{$i}}] != 1.5 {
		t.Errorf("Expected {{$param.Name}}[%d] to be 1.5, got '%v'", {{$i}}, cmd.{{$param.Name}}[{{$i}}])
	}
					{{- else if $param.IsBool}}
	if cmd.{{$param.Name}}[{{$i}}] != true {
		t.Errorf("Expected {{$param.Name}}[%d] to be true, got '%v'", {{$i}}, cmd.{{$param.Name}}[{{$i}}])
	}
					{{- else if $param.IsDuration}}
	if cmd.{{$param.Name}}[{{$i}}] != 1 * time.Second {
		t.Errorf("Expected {{$param.Name}}[%d] to be 1s, got '%v'", {{$i}}, cmd.{{$param.Name}}[{{$i}}])
	}
					{{- end}}
				{{- end}}
			{{- end}}
		{{- else}}
			{{- if .HasPointer}}
				{{- if eq .BaseType "string"}}
	if cmd.{{.Name}} == nil {
		t.Errorf("Expected {{.Name}} to be non-nil")
	} else if *cmd.{{.Name}} != "test" {
		t.Errorf("Expected {{.Name}} to be 'test', got '%v'", *cmd.{{.Name}})
	}
				{{- else if or (eq .BaseType "int") (eq .BaseType "int64")}}
	if cmd.{{.Name}} == nil {
		t.Errorf("Expected {{.Name}} to be non-nil")
	} else if *cmd.{{.Name}} != 1 {
		t.Errorf("Expected {{.Name}} to be 1, got '%v'", *cmd.{{.Name}})
	}
				{{- else if eq .BaseType "float64"}}
	if cmd.{{.Name}} == nil {
		t.Errorf("Expected {{.Name}} to be non-nil")
	} else if *cmd.{{.Name}} != 1.5 {
		t.Errorf("Expected {{.Name}} to be 1.5, got '%v'", *cmd.{{.Name}})
	}
				{{- else if .IsBool}}
	if cmd.{{.Name}} == nil {
		t.Errorf("Expected {{.Name}} to be non-nil")
	} else if *cmd.{{.Name}} != true {
		t.Errorf("Expected {{.Name}} to be true, got '%v'", *cmd.{{.Name}})
	}
				{{- else if .IsDuration}}
	if cmd.{{.Name}} == nil {
		t.Errorf("Expected {{.Name}} to be non-nil")
	} else if *cmd.{{.Name}} != 1 * time.Second {
		t.Errorf("Expected {{.Name}} to be 1s, got '%v'", *cmd.{{.Name}})
	}
				{{- end}}
			{{- else}}
				{{- if eq .Type "string"}}
	if cmd.{{.Name}} != "test" {
		t.Errorf("Expected {{.Name}} to be 'test', got '%v'", cmd.{{.Name}})
	}
				{{- else if or (eq .Type "int") (eq .Type "int64")}}
	if cmd.{{.Name}} != 1 {
		t.Errorf("Expected {{.Name}} to be 1, got '%v'", cmd.{{.Name}})
	}
				{{- else if eq .Type "float64"}}
	if cmd.{{.Name}} != 1.5 {
		t.Errorf("Expected {{.Name}} to be 1.5, got '%v'", cmd.{{.Name}})
	}
				{{- else if eq .Type "bool"}}
	if cmd.{{.Name}} != true {
		t.Errorf("Expected {{.Name}} to be true, got '%v'", cmd.{{.Name}})
	}
				{{- else if .IsDuration}}
	if cmd.{{.Name}} != 1 * time.Second {
		t.Errorf("Expected {{.Name}} to be 1s, got '%v'", cmd.{{.Name}})
	}
				{{- end}}
			{{- end}}
		{{- end}}
	{{- else}}
		{{- if .HasPointer}}
			{{- if eq .BaseType "string"}}
	if cmd.{{.Name}} == nil {
		t.Errorf("Expected {{.Name}} to be non-nil")
	} else if *cmd.{{.Name}} != "test" {
		t.Errorf("Expected {{.Name}} to be 'test', got '%v'", *cmd.{{.Name}})
	}
			{{- else if or (eq .BaseType "int") (eq .BaseType "int64")}}
	if cmd.{{.Name}} == nil {
		t.Errorf("Expected {{.Name}} to be non-nil")
	} else if *cmd.{{.Name}} != 1 {
		t.Errorf("Expected {{.Name}} to be 1, got '%v'", *cmd.{{.Name}})
	}
			{{- else if eq .BaseType "float64"}}
	if cmd.{{.Name}} == nil {
		t.Errorf("Expected {{.Name}} to be non-nil")
	} else if *cmd.{{.Name}} != 1.5 {
		t.Errorf("Expected {{.Name}} to be 1.5, got '%v'", *cmd.{{.Name}})
	}
			{{- else if .IsBool}}
	if cmd.{{.Name}} == nil {
		t.Errorf("Expected {{.Name}} to be non-nil")
	} else if *cmd.{{.Name}} != true {
		t.Errorf("Expected {{.Name}} to be true, got '%v'", *cmd.{{.Name}})
	}
			{{- else if .IsDuration}}
	if cmd.{{.Name}} == nil {
		t.Errorf("Expected {{.Name}} to be non-nil")
	} else if *cmd.{{.Name}} != 1 * time.Second {
		t.Errorf("Expected {{.Name}} to be 1s, got '%v'", *cmd.{{.Name}})
	}
			{{- end}}
		{{- else}}
			{{- if eq .Type "string"}}
	if cmd.{{.Name}} != "test" {
		t.Errorf("Expected {{.Name}} to be 'test', got '%v'", cmd.{{.Name}})
	}
			{{- else if or (eq .Type "int") (eq .Type "int64")}}
	if cmd.{{.Name}} != 1 {
		t.Errorf("Expected {{.Name}} to be 1, got '%v'", cmd.{{.Name}})
	}
			{{- else if eq .Type "float64"}}
	if cmd.{{.Name}} != 1.5 {
		t.Errorf("Expected {{.Name}} to be 1.5, got '%v'", cmd.{{.Name}})
	}
			{{- else if eq .Type "bool"}}
	if cmd.{{.Name}} != true {
		t.Errorf("Expected {{.Name}} to be true, got '%v'", cmd.{{.Name}})
	}
			{{- else if .IsDuration}}
	if cmd.{{.Name}} != 1 * time.Second {
		t.Errorf("Expected {{.Name}} to be 1s, got '%v'", cmd.{{.Name}})
	}
			{{- end}}
		{{- end}}
	{{- end}}
	{{- end}}
	{{- end}}
	{{else}}
	// No function defined for root command, so Execute expects a subcommand.
	// We can test that it fails with unknown command or usage.
	err = cmd.Execute([]string{"unknown-command"})
	if err == nil {
		t.Error("Expected error for unknown command, got nil")
	}
	{{end}}
}

func TestRoot_ExecuteHelpAndUnknownFlags(t *testing.T) {
	cmd, err := NewRoot("test", "", "", "")
	if err != nil {
		t.Fatalf("Failed to create root command: %v", err)
	}

	if err := cmd.Execute([]string{"--help"}); err != nil {
		t.Errorf("--help returned an error: %v", err)
	}
	if err := cmd.Execute([]string{"-h"}); err != nil {
		t.Errorf("-h returned an error: %v", err)
	}
	if err := cmd.Execute([]string{"--not-a-real-flag"}); err == nil {
		t.Error("expected an error for an unknown long flag")
	}
	if err := cmd.Execute([]string{"-?"}); err == nil {
		t.Error("expected an error for an unknown short flag")
	}
}
