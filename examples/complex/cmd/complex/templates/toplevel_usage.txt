Usage: complex toplevel <subcommand> [arguments]

This command demonstrates a top level command with a string argument.
It prints the name provided.

Subcommands:
    nested     A nested command
    help         Print this help message
    usage        Print this usage message

Flags:
    --name, -n string   The name to greet (default: world)
