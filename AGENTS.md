
* **Do NOT downgrade dependencies** unless strictly necessary and verified. Specifically, do not downgrade `golangci-lint` or `golangci-lint-action` to older versions. Use `install-mode: goinstall` if there is a Go version mismatch with the pre-compiled binary.
